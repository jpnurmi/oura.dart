enum BedtimeStatus {
  notEnoughData,
  lowSleepScores,
  idealBedtimeAvailable,
}

enum Gender { male, female, other }

enum RestModeState {
  off,
  enteringRestMode,
  restMode,
  enteringRecovery,
  recovering,
}
